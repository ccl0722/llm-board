# 设计系统规范 · Graphite Board V4

> 这套看板的**设计契约**。改视觉前先读第 2 节（配色约束）与第 10 节（禁令清单）；
> 改数据展示前先读第 8 节（数据诚实性规则）——这一节是本版新增的，也是最容易违反的。

---

## 1. 设计定位

**个人研究工具，不是宣传页。**暖石墨底、象牙白字、克制的交互色、以表格为主的信息承载。

期望的第一印象：

> 「一个能长时间盯着看的行业数据终端」

而不是：

> 「一份 AI 生成的蓝色科技 Dashboard」或「一本数据主题的宣传杂志」

参考坐标（只取气质，不复制任何产品）：

| 来源 | 取什么 |
| --- | --- |
| Linear | 克制的边框与悬停反馈、低对比表面 |
| Bloomberg Terminal | 信息密度、等宽数字、表格化布局 |
| Vercel | 黑白秩序、靠排版而非颜色建立层级 |
| 高端研究报告 | 少量衬线标题、编辑级留白、暖灰纸感 |

**目标三个词**：`institutional`（机构感）· `editorial`（编辑感）· `information-first`（信息优先）

---

## 2. 色彩系统 ★核心

### 2.1 设计令牌（`src/css/01-tokens.css`）

所有颜色只允许从令牌取值。**不要在组件里写死新的十六进制色。**

#### 表面 Foundation —— 层级靠明度差，不靠边框堆叠

| 变量 | 色值 | 用途 |
| --- | --- | --- |
| `--bg` | `#0E100E` | 页面底色（暖石墨，不是死黑） |
| `--bg-rail` | `#121512` | 侧栏、吸顶条、抽屉、浮层 |
| `--surface` | `#171A17` | 表格与面板主表面 |
| `--surface-2` | `#1C201C` | 表头、内嵌块、静态强调 |
| `--surface-3` | `#22261F` | hover / 选中底 |
| `--line` | `rgba(238,234,226,.085)` | 常规分割线 |
| `--line-2` | `rgba(238,234,226,.155)` | 强调边框 |
| `--line-3` | `rgba(238,234,226,.26)` | 复选框边框、条形轨道填充 |

> 所有线色都是**象牙白透明化**（238,234,226），不是纯白也不是冷灰。
> 这是暖调质感的关键，不要改成 `rgba(255,255,255,...)`。

#### 文字 Typography —— 三级都比 V3 提亮，中文正文优先可读性

| 变量 | 色值 | 用途 |
| --- | --- | --- |
| `--ink` | `#EFEDE6` | 主文字 |
| `--ink-2` | `#B7B4AA` | 正文说明、次级信息（V3 是 `#AAA79E`） |
| `--ink-3` | `#8D8B82` | 标签、脚注、单位（V3 是 `#706F69`，太暗） |
| `--ink-hi` | `#DCD7CA` | 中性强调：表格里的数字与关键值 |

**最小字号 10.5px，且只用于列头单位与口径标签。**正文不低于 12.5px。

#### 交互主色 —— 鼠尾草灰绿

| 变量 | 色值 | 用途 |
| --- | --- | --- |
| `--sage` | `#A8B39C` | 选中态、复选框、焦点环、当前导航项、榜首条形 |
| `--sage-hi` | `#C2CBB6` | 悬停加深 |
| `--sage-dim` / `--sage-line` | 透明化 | 选中行底 / 选中边框 |

#### 情报强调 —— 黄铜

| 变量 | 色值 | 用途 |
| --- | --- | --- |
| `--brass` | `#C8A96C` | 「本期变化」**专用**：变化条目的影响行、本期新增徽章、时间线最新节点、关注星标 |
| `--brass-dim` / `--brass-line` | 透明化 | 底色 / 边框 |

#### 语义色

| 变量 | 色值 | 用途 |
| --- | --- | --- |
| `--good` | `#8AAE90` | **最优值**（且只有最优值） |
| `--bad` | `#C97C74` | 风险 / 移除操作 |
| `--warn` | `#C7A063` | **待核实 / 口径提示 / 官方自报**标记 |

> **V3 遗留的 12 个 `--c-*` 厂商色已全部删除。**品牌识别只由真实 logo 承担，
> 数据条一律中性灰，不再用厂商色染色。这也顺带干掉了原版 93 处内联 `style="--bc:..."`。

### 2.2 色彩配比（硬约束）

```
94%  neutral graphite + ivory      ← 表面、文字、边框、分割线
 3%  muted sage  (#A8B39C)         ← 交互：选中 / 激活 / 焦点
 2%  brass       (#C8A96C)         ← 本期变化 / 关注
 1%  semantic     good / bad / warn ← 最优值、风险、待核实
```

### 2.3 BLUE BUDGET（蓝色预算）★

> **Blue is not the UI identity.**

蓝色**不得**承担：品牌主色、区块标题、导航激活态、筛选选中态、对比选中态、
本期变化、时间线、提示框、按钮、卡片边框、通用图表高亮。

蓝色**只允许**出现在：**公司真实 logo 自带的品牌色**（DeepSeek 蓝鲸、Meta 无限符、腾讯、Gemini 渐变等）。

验收：全局搜索下列色值，除 `assets/logos/*.svg` 外必须 0 命中。

```
#5B9CF5  #6EA8FE  #8AB8FF  #91B9FF  #B8D4FF  #4A8AE0
#3DE0A4  #5ED0FF  #7DB8FF  #2f81f7  #4285f4
rgba(91,156,245   rgba(110,168,254
```

当前状态：**CSS 与 JS 中 0 残留。**

---

## 3. 字体系统

| 变量 | 字体栈 | 用途 |
| --- | --- | --- |
| `--sans` | Inter → PingFang SC → 微软雅黑 | 正文、表格、标签 —— **默认一切** |
| `--mono` | SF Mono → JetBrains Mono → Consolas | 数字、价格、日期、单位 |
| `--serif` | Noto Serif SC → 思源宋体 → Georgia | **只用在四个视图标题与侧栏品牌名上** |

**衬线字体的用量是有上限的。**全站只有 5 处：侧栏品牌名 + 四个视图 `<h1>`。
区块标题（`.sec-head h2`）用无衬线 16px / 650 —— 密集的数据界面不该长得像宣传杂志。

**所有数字必须等宽对齐**：`.num` / `.mono` 类同时设 `font-variant-numeric: tabular-nums`
与 `font-feature-settings:"tnum" 1`。表格数字列一律 `text-align:right` + `white-space:nowrap`。

**层级只靠字号 / 字重 / 字距 / 衬线对比建立，不靠颜色。**

| 层级 | 规格 |
| --- | --- |
| 视图标题 | serif 25px / 700 |
| 区块标题 | sans 16px / 650 |
| 正文 | sans 13.3–15px / 400，行高 1.62–1.78 |
| 表格 | sans 13.5px，行高 1.45 |
| 列头 | mono 10.5px，`letter-spacing:.055em`，大写 |
| 单位 / 口径 | sans 10.5–11px，`--ink-3` |

---

## 4. 布局与间距

| 项 | 值 |
| --- | --- |
| 骨架 | `grid: 218px 侧栏 + 内容`，内容最大宽 1240px |
| 内容边距 | 桌面 28px / 平板 22px / 手机 16px（`--pad`） |
| 圆角 | 表面 `8px`，容器 `12px`，标签 `4px`，胶囊 `99px` |
| 表格单元格 | `padding: 8px 11px` |
| 区块间距 | `.sec` 上边距 40px（手机 30px） |
| 响应式断点 | `1180px`（侧栏转顶栏）/ `1080px`（概览两栏转一栏）/ `900px` / `720px` |

**留白服务于阅读，不通过放大卡片、拉长页面制造「高级感」。**
重设计后文档高度从 **29060px 降到约 2600px**（概览视图），信息量没有减少，只是按重要程度分层了。

### Grid 的一个坑

覆写 `grid-template-columns` 时必须写 `minmax(0,1fr)` 而不是 `1fr` ——
`1fr` 等价于 `minmax(auto,1fr)`，子元素的 min-content 会把列撑破，在手机上直接导致页面横向溢出。
本项目所有 grid 覆写都用 `minmax(0,1fr)`。

### 宽表格

宽表一律放进 `.tbl-wrap`（`overflow:auto`），**在自身容器内横向滚动，不允许把页面撑宽**。
`regression-test.js` 会在 1440 / 1280 / 390 三档 × 四个视图上检查页面级横向溢出。

---

## 5. 组件清单

页面结构在 `src/index.html`，**内容几乎全部由 `app.js` 从 `data.js` 渲染**，
改组件样式时先确认它是静态骨架还是 JS 模板。

| 组件 | 关键类名 | 渲染位置 |
| --- | --- | --- |
| 侧栏 | `.rail` `.rail-brand` `.rail-nav` `.rail-foot` | 骨架 + `renderNav()` |
| 顶部工具条 | `.topbar` `.search` | 骨架 |
| 快照状态条 | `.snapbar` | 骨架 + `renderOverview()` |
| 本期变化 | `.changes` `.chg` `.chg-meta` `.impact` `.mref` | `renderChanges()` |
| 概览两栏 | `.ov-top` `.ov-side` | 骨架 |
| 数据表 | `.tbl-wrap` `table.tbl` `.mcell` `.ck` `.star` `.th-sub` | `renderModelTable()` / `miniTable()` |
| 筛选条 | `.filters` `.fgroup` `.pill` `.fstatus` `.pop-menu` | `renderFilters()` |
| 场景推荐 | `.scenes` `.scene` | `renderOverview()` |
| 对比托盘 | `.tray` `.cchip` `.cslot` | `renderCompareUI()` |
| 深度对比 | `.cmp-wrap` `table.cmp` `.rowh` `.cmp-h` `.cval` `.cnote` `tr.grp` | `compareTable()` |
| 价格 / 规格 / Agent 矩阵 | `table.tbl` `table.mx` | `renderPricing()` |
| 散点 | `.chart` `.pt` `.gl` `.gy` `.gx` `.ax-x` `.ax-y` | `drawChart()` |
| 订阅卡 | `.subs` `.plan` | `renderPricing()` |
| 时间线 | `.tl` `.tli` | `renderIntel()` |
| 可展开列表 | `.rows` `.row-x`（原生 `<details>`） | `renderIntel()` |
| 引用 | `.quotes` `.quote` | `renderVoices()` |
| 抽屉 | `.drawer` `.dh` `.db` `.df` `.dsec` `.dspecs` `.dfacts` | `openModel()` / `openVendor()` |
| 对话框 | `.dialog` | 骨架 |
| 提示 | `.toast` | `toast()` |
| 空状态 | `.empty` | 各渲染函数 |

> **`.sub` 是个陷阱。**它在 `.mcell`、`.sec-head`、`table.mx` 里都用作「次级文字」。
> 订阅卡曾经也叫 `.sub`，导致一条 `padding:15px 17px` 泄漏到全站每一个 `.sub`（表格行高直接翻倍）。
> 订阅卡现在叫 `.plan`。**加新组件时不要用 `.sub`、`.note`、`.row` 这类通用词做顶层类名。**

---

## 6. 品牌标识系统 ★

### 6.1 架构

```
BRANDS 注册表（data.js，唯一数据源）
        │  logoHTML(key, size) / orgName(key)
        ├──→ 模型库行 / 概览速览 / 价格表 / 规格表 / Agent 矩阵
        ├──→ 厂商筛选 pill
        ├──→ 对比表列头 · 对比托盘 chip
        ├──→ 抽屉头部（模型与厂商）
        └──→ 来源表
```

### 6.2 规则

1. **公司标识与模型家族标识分开登记。**厂商档案用公司标识，模型行用模型品牌标识；
   模型没有独立标识时（DeepSeek / MiniMax / Mistral / Nemotron / MiMo / Muse / GLM）
   回落到所属公司标识，**全站保持一致**。
2. **禁止字母头像**、Emoji、AI 生成或自绘的近似图形冒充真实标识。
   找不到可核实的标识时，`BRANDS` 里 `logo` 留空，界面自动退化为**品牌全名文字**（`.logo-txt`），并在 `SOURCES.md` 记缺口。
3. **承载区固定高度、宽度自适应**：`.logo{height:19px;width:19px}`，`img` 用
   `max-width/height:100%; width:auto; height:auto; object-fit:contain`。
   细长标识（如 Motif 的 59:36）在 `BRANDS` 里标 `wide:true`，容器加宽到 26 / 36 / 48px，避免被压得过小。
   尺寸档：行内 `19px` · `lg` `26px` · `xl` `34px`。
   > ⚠️ 历史事故：一次整块替换 CSS 误删了尺寸约束，小米的 MI 字形被放大成巨型图标。
   > **任何整块替换 CSS 的操作，都必须先盘点被删除块内的全部选择器。**
4. **单色标识用官方的深色背景版本**（反白到 `#EDEAE3`），不套白色方块。
   彩色标识原样保留，**不拉伸、不裁切、不改色、不改比例**。
5. **本地化，不热链。**所有 SVG 存在 `assets/logos/`，来源逐个登记在 `SOURCES.md`
   （品牌、来源、原始链接、获取日期、做过什么处理）。

### 6.3 新增一家厂商

1. 下载官方 SVG 到 `assets/logos/<key>.svg`
2. `data.js` 的 `BRANDS` 加一行：`foo:{name:'Foo AI', logo:'assets/logos/foo.svg'}`
3. `MODELS` 里把对应模型的 `org` / `brand` 指向该 key
4. `assets/logos/SOURCES.md` 补一行来源记录

---

## 7. 数据可视化

| 图形 | 规则 |
| --- | --- |
| 行内条形（表格内） | 中性 `--line-3`，最优值用 `--good`。表格里**不用厂商色** |
| 排行柱状图 | **厂商色**（`LAB_COLORS`，16 色），柱下挂真实品牌标识；大色块按 `fill-opacity:.82` 向底色收敛 |
| 散点 | 点是中性灰，在对比中的模型高亮 `--sage`；**必须有坐标轴标签、单位、对数刻度说明与数据口径**；密集区做标签避让，被隐藏的标签靠 `title` 悬停补齐 |
| 最优值 | 文字染 `--good` + 「最优」小标签；**只在满足第 8 节全部条件时才出现** |
| 热力 / 综合分 / 雷达 | **不做。**没有可靠数据支撑就用清晰的表格 |

### 厂商色为什么可以有 16 个 ★

通用数据可视化规范给分类色设了 8 个上限，理由是「超过 8 个，相邻色在色盲视角下无法区分」。
本项目**有意突破这条**，依据是：

1. **颜色在这里不承担识别。** 每根柱子下面挂着该厂商的真实品牌标识，旁边有名称，
   下方有图例、悬停提示，以及一张完整的表格。颜色只负责「一眼看出同一家聚在哪儿」。
   规范本身也允许 ΔE 6–8 的色对「在有第二编码时」使用 —— 品牌标识就是最强的第二编码。
2. **实测结论要如实记录**（`LAB_COLORS` 注释里也写了同样的话）：
   16 色对石墨底 `#171A17` 的对比度**全部通过**（≥3:1）；
   但**两两可分不通过** —— 最糟一对正常视觉 ΔE 7.4，色盲视角下 MiniMax 品红与 Motif 绿完全重合。
   **这不是调参能解决的，16 个分类色在数学上就不可能两两可分。**
3. **因此界面上任何一处都不得只靠颜色读懂。** 图表脚注里明写了这句话，
   并提供「单色」开关：切换后所有柱子同色、图例收起，需要精确区分时用它。

调色板的构造方式记录在 `data.js` 的 `LAB_COLORS` 注释里（OKLCH 固定彩度、
色相按品牌色就近分配、明度随色相自然起伏并局部交替）。改动它时请重跑校验，
并同步更新这里与图表脚注里的实测结论 —— **不要只改颜色不改那句话。**

---

**图表只有在帮助理解比较时才保留。**本版有两张：**指标排行柱状图**与**智能指数 × 单任务实测成本散点**，
散点两轴同源（均为 Artificial Analysis 实测），横轴取对数刻度，并画出帕累托前沿。
柱状图的几何照通用规范：柱宽 ≤24px、顶端 4px 圆角底端切平、相邻留 2px 表面间隙、
网格线为 1px 实线（**不用虚线**）、散点标记 r≥5 且带 2px 表面描边、
**数值标签只给前三名与已选中的**（不是每根都标）。
「单任务成本」是有明确定义的真实指标 —— 跑完一道智能指数任务的加权平均花费，含推理 token 与缓存命中，
所以它比标价更接近真实账单。仍然明写「这是跑评测任务的成本，不等于你自己业务的单次调用成本」。

---

## 8. 数据诚实性规则 ★本版新增，优先级高于视觉

这一节是硬规则，违反它比配色出错严重得多。

1. **三个日期必须分开显示**：数据快照日 / 榜单基准日 / 页面生成日。
2. **没有自动更新管线，就不准声称「每日自动更新」**，也不准做实时状态点、脉冲呼吸灯之类的暗示。
   按钮按实际动作命名 ——「复制更新指令」而不是「更新」。
3. **缺失值显示「暂无数据」或「待核实」，绝不写 0**，排序时永远排最后，不参与任何计算。
4. **参数量 ≠ 上下文窗口 ≠ 最大输出长度**，三者分列，互不换算。
5. **不同 Benchmark 版本不得混排**（Terminal-Bench 2.1 与 4.0 是两个独立指标）。
   **指标本身也会换代** —— 2026-09 这一期里 AAII（v4.1.1 → v4.3）、Terminal-Bench（3.0 → 4.0）、
   编码 Agent 指数（v1.4 → v1.5）同时换了尺。换代时必须：
   ① 在 `METRICS` 的 `warn` 里写明不可跨版本比较；
   ② 在 `CONFLICTS` 里留一条「已定口径」记录；
   ③ **不要**把新旧分数放进同一条趋势线或涨跌标记。
   `app.js` 的 `initCols()` 会自动丢弃 localStorage 里残留的旧指标 key，不必手动清缓存。
6. **每个数值标注取值类型**：独立评测 / 官方自报 / 估算 / 编辑判断。
   密集表格里只标「估算」（否则满屏「官方自报」会淹掉真正的提示），对比表与详情抽屉标全部。
7. **最优值动态计算，永不硬编码。**四条否决规则见 README 第四节。
   > ⚠️ 历史事故：V3 的 `renderVs()` 用 `p.aa==='63'`、`p.swe.indexOf('97')===0` 这类字面量判断谁最优，
   > 数据一变就失效。**现在页面上不存在任何硬编码的分数或名次。**
8. **同一模型在不同区块数值打架时，两个说法都记进 `CONFLICTS`**，页面有专门的待核实表，不擅自选一个。
9. **来源要尽量贴近具体数据。**只有站点首页链接时，必须写明「非逐条证据」，不能包装成已核实。
10. **没有历史快照序列，就不生成涨跌曲线、排名变化或任何趋势图。**
    即使以后攒够了快照，跨基准版本的数据也不能连成一条线。
13. **基准被归档停更时，要把「空白」和「缺数据」区分开。** 例如 SWE-bench Verified 于 2026-09-01 归档，
    此后发布的模型永远不会有分数 —— 这要写进 `METRICS.warn`，否则用户会以为是采集遗漏。
11. **场景建议必须写明依据**（用了哪个指标、值是多少），证据不足时展示「待比较候选」，
    不制造「最强」「第一选择」这类绝对标签。
12. **API 价格统一币种与单位**（美元 / 每百万 token），输入、输出、缓存分列，
    促销 / 峰谷 / 渠道折扣写进计价方式；**订阅价与 API 用量价格分开展示，不可相加**。

---

## 9. 行为层架构（`app.js`）

```
┌ 数据层（data.js）────────────────────────────────────┐
│ META        抓取日 / 生成日 + 无管线声明                │
│ BRANDS  26  公司 / 模型品牌 / 生态品牌标识（唯一源）     │
│ SOURCES  8  来源登记（名称 / 类型 / 读取日 / 链接性质）  │
│ METRICS 20  指标定义：方向 / 口径 / 来源 / 横比风险      │
│ MODELS  41  模型档案，每个数值都带 kind + note          │
│ SCENES   4  用途 → 参考指标（候选由数据算，不写死）      │
│ CHANGES 15 · VENDORS 16 · TIMELINE 26 · NOTES 18       │
│ VOICES  22 · CONFLICTS 14 · SUBSCRIPTIONS · AGENT_*    │
└────────────────────────────────────────────────────────┘
                        ↓
┌ 渲染层 ────────────────────────────────────────────────┐
│ renderNav / renderOverview / renderChanges / renderFav │
│ renderFilters / renderModelTable   ← 模型库核心         │
│ renderCompareUI / renderCompare / compareTable          │
│ renderPricing / drawChart / renderIntel / renderVoices  │
└────────────────────────────────────────────────────────┘
                        ↓
┌ 交互层 ────────────────────────────────────────────────┐
│ hash 路由（4 视图）· 搜索 · 筛选 · 排序 · 列设置         │
│ 加入/移出对比（上限 4，超限提示不清空）· 只看差异        │
│ 关注模型（localStorage）· 抽屉 · 更新指令对话框          │
│ 键盘：Esc 关闭 · Enter/空格激活 · 抽屉内 Tab 焦点环      │
└────────────────────────────────────────────────────────┘
```

**状态持久化**：只有 `fav` / `cmp` / `cols` 三项进 `localStorage`，
全部包在 try/catch 里 —— `file://` 与隐私模式下读写失败必须静默降级，不能崩。

**「筛选」和「加入对比」是两件事**，界面上分别是厂商 pill / 条件 pill 与行首复选框，
选中态分别是 pill 高亮与整行 `--sage-dim` 底色。不要把两者合并。

---

## 10. 禁令清单

页面**绝对不要**长成以下任何一种：

- ❌ 蓝色科技 Dashboard / 蓝紫渐变 AI SaaS
- ❌ neon cyberpunk / glowing terminal / 大面积玻璃效果
- ❌ 渐变文字、彩色阴影、发光、夸张悬停浮动
- ❌ 厂商色染整张卡片或整条 bar
- ❌ 字母头像、Emoji 冒充厂商标识
- ❌ 所有内容都塞进同样的圆角卡片（该用表格就用表格）
- ❌ 装饰性 Emoji、无意义的英文小标题、重复徽章、无限循环动画
- ❌ 首屏被宣传式大标题和「追踪 21 家实验室」这类无助于选型的统计占满
- ❌ 把整页做成纯灰（**目标不是去色，是把颜色留给真正有语义的地方**）

**色彩的生命力来自这四项**，而不是 UI 框架自己制造颜色：
① 真实品牌标识 ② 黄铜（本期变化）③ 鼠尾草（交互）④ 语义绿/红/黄（最优、风险、待核实）

---

## 11. 可访问性与系统偏好

- `prefers-reduced-motion: reduce` → 全站动画与过渡降到 0.001ms，`scroll-behavior:auto`
- `@media print` → 隐藏侧栏、工具条、托盘、抽屉、对话框，底色转白，便于导出 PDF
- 焦点环：`:focus-visible` 统一 2px `--sage`，offset 2px
- 语义：视图用 `<section aria-labelledby>`，导航用 `aria-current="page"`，
  排序表头 `aria-sort`，复选与开关 `aria-pressed`，提示区 `role="status" aria-live="polite"`
- 键盘：Esc 关闭抽屉与对话框；Enter / 空格激活非 button 的可点元素；抽屉内 Tab 焦点环
- 跳转链接：`.skip` 跳到主要内容
- **图标类 `<img>` 一律 `alt=""` + 容器 `aria-hidden="true"`**（品牌名已有文字），避免屏幕阅读器重复朗读

---

## 12. 验收清单

改完对着这份清单自查（建议 1440px 视口）：

**数据诚实性（优先级最高）**
- [ ] 三个日期分开显示，没有「自动更新」「实时」之类的暗示
- [ ] 缺失值是「暂无数据」，排序时在最后，没有按 0 参与计算
- [ ] 参数量 / 上下文 / 最大输出没有互相串
- [ ] 不同 Benchmark 版本没有混排
- [ ] 最优值是算出来的，全文搜不到硬编码的分数比较
- [ ] 新增的冲突进了 `CONFLICTS`

**视觉**
- [ ] 第一眼不像蓝色 Dashboard；蓝色只在 logo 里
- [ ] 衬线字体仍然只出现在 5 处
- [ ] 数字等宽且按列对齐
- [ ] 选中（鼠尾草）/ 本期（黄铜）/ 最优（绿）/ 待核实（黄）四者一眼可分辨
- [ ] 没有新增无限动画、发光、渐变文字

**布局**
- [ ] 1440 / 1280 / 390 三档无页面级横向溢出
- [ ] 宽表在 `.tbl-wrap` 内滚动
- [ ] 覆写 grid 时用了 `minmax(0,1fr)`

**代码**
- [ ] 新组件没有占用 `.sub` / `.note` / `.row` 这类通用类名
- [ ] 新增品牌进了 `BRANDS` + `SOURCES.md`
- [ ] 改完 `src/` 跑过 `python build.py`
- [ ] `tools/regression-test.js` 退出码为 0

---

*本规范与代码同步维护。改视觉前先读第 2 节，改数据展示前先读第 8 节。*
